# AIS Ticket Concierge — Memory Bank

## Project
- Path: C:\Users\emikes\TicketManager (in-place conversion of the original npm-workspaces client/server repo)
- App name: AIS Ticket Concierge
- Environment: 946e5045-3200-eb26-9160-3fe967a6b21c (org5c824bd0.crm.dynamics.com)
- App URL: https://apps.powerapps.com/play/e/946e5045-3200-eb26-9160-3fe967a6b21c/app/1323f8ca-47e5-402e-9657-45f8651af4dc

## Why this migration
Originally scaffolded as a generic Express+SQLite/Drizzle+JWT monorepo (packages/client, /server, /shared) because the code-app requirement wasn't communicated up front. Converting in place to a real Power Apps Code App: client-only React/Vite app, Dataverse for data, no custom backend, no raw fetch (connector-only). Full plan at the time of the pivot: C:\Users\emikes\.claude\plans\async-munching-crab.md (read this for full architecture rationale — Dataverse table design, seat-assignment concurrency approach, AI Builder GPT-4.1 Mini plan, etc).

## Completed steps
- [x] Removed old packages/client, packages/server, packages/shared (Express/SQLite/JWT stack) — recoverable via `git show HEAD:packages/...` since it was committed before removal, or `git log` on the original commit 57615aa.
- [x] Scaffolded Power Apps Code App template in place (`npx degit microsoft/PowerAppsCodeApps/templates/vite . --force`), `npm install`.
- [x] `npx power-apps init -n 'AIS Ticket Concierge' -e 946e5045-3200-eb26-9160-3fe967a6b21c` — succeeded after user re-authenticated (first attempts failed with `ServiceToServiceEnvironmentNotFound` due to a stale/wrong signed-in tenant).
- [x] Baseline build + deploy (`npm run build`, `npx power-apps push`) — bare template is live at the App URL above.
- [x] Added `vitest`, ported the pure scoring engine (`src/scoring/{engine,factors,types}.ts`, `src/domain/{enums,constants,scoring-types}.ts`) from `packages/server/src/scoring/*` — IDs changed from SQLite integers to Dataverse GUID strings throughout. Ported `engine.test.ts` too (also adapted to string IDs) — all 5 tests pass. `npm run build` clean.

## Dataverse tooling setup (for creating new tables)
- Azure CLI is NOT installed on this machine and the user can't install it right now — do not suggest the `az`-based path in add-dataverse's api-authentication-reference.md.
- Instead: PAC CLI (already installed, already authenticated as emikes@ais-now.com to org "AIS Development" / env 946e5045-3200-eb26-9160-3fe967a6b21c) + the `dataverse-plugins:dv-connect` skill flow. Installed globally: `@microsoft/dataverse` npm CLI, Python packages (azure-identity, requests, PowerPlatform-Dataverse-Client, pandas, msal, msal-extensions).
- IMPORTANT PATH GOTCHA: `C:\Users\emikes\AppData\Roaming\npm` is not on PATH in this shell session (neither bash nor the PowerShell session used so far) — invoke the CLI via full path `/c/Users/emikes/AppData/Roaming/npm/dataverse` (bash) until a fresh terminal picks up PATH.
- `dataverse auth create --environment https://org5c824bd0.crm.dynamics.com` succeeded (tenant a22e51ee-6f67-49b3-b98c-0e3a97d49b8a). `dataverse auth who` and `pac org who` both confirm. **`python scripts/auth.py` currently fails** ("silent token acquisition failed") even right after a fresh `dataverse auth create` — unresolved; the Python SDK path (used for advanced dv-metadata ops like choice columns/relationships) may need a fresh investigation. Basic MCP-based table/column creation does not depend on this.
- `.env` created at repo root (gitignored) with DATAVERSE_URL/TENANT_ID/MCP_CLIENT_ID/SOLUTION_NAME=AISTicketConcierge etc.
- Dataverse MCP server registered for Claude Code: `dataverse mcp allow 0c412cc3-...` (was disabled, now enabled) + `claude mcp add --scope project dataverse-org5c824bd0 -t stdio ...` → written to `.mcp.json`. `--validate` confirms **GA endpoint is valid** (Preview 403 is expected/fine, ignore it). **Requires a Claude Code session restart to activate the MCP tools** — do this before attempting any Dataverse table creation.
- Did NOT copy the dv-connect `templates/CLAUDE.md` workspace template to repo root — this repo is much broader than a pure Dataverse-schema project (full Code App), and this memory-bank.md + the plan doc already cover the same continuity purpose.

## Data sources — Phase 1 DONE
The environment's actual default publisher prefix is **`cr9cd_`**, not the `ais_` used in the plan doc's prose — cosmetic only, same design. Created via the `dataverse-org5c824bd0` MCP server's `create_table` (NOT the Azure-CLI/PowerShell path in add-dataverse's api-authentication-reference.md — that's unavailable here, see below):
- `cr9cd_team` (abbreviation, sport, venue, home_games_per_season, default_platform choice, official_url, default_tickets_per_game, native statecode for isActive) — seeded with the 4 real teams + synthetic "Custom Events" team.
- `cr9cd_season` (team lookup, start/end date, status choice)
- `cr9cd_game` (season lookup, game_date, opponent, promotions, notes, status choice, total_seats, premium_score, title, description, kind choice)
- `cr9cd_seat` (game lookup, section, row, seat_number, is_ada, status choice)

All 4 wired into the Code App via `npx power-apps add-data-source -a dataverse -t cr9cd_<table> -u https://org5c824bd0.crm.dynamics.com` → generated services in `src/generated/services/Cr9cd_*Service.ts`. `npm run build` clean.

**Known limitation of the `create_table`/`update_table` MCP tools**: no `autonumber` column type, and no native N:N relationship creation (lookup/N:1 only) — both were in the original plan (publicId autonumber columns, native N:N between ticketrequest↔contact). Since `python scripts/auth.py` still fails (see below), the SDK path for these isn't available yet either. **Decision needed at Phase 2**: fall back to a plain text/GUID publicId column (no autonumber) and a manual join table for request↔contact (like the original SQLite `request_contacts`), OR debug the Python auth failure first. Leaning toward the fallback to avoid blocking on the Python issue.

An `account` standard-table data source was added early as a connectivity probe (before table creation was figured out) — harmless, and actually needed anyway in Phase 3 for CRM, so left in place rather than removed.

## Phase 2 — DONE
Remaining core tables created (same MCP `create_table` mechanism as Phase 1): `cr9cd_contact_beneficiary` (crm_contact/crm_account lookups to standard `contact`/`account` tables — NOTE: this environment has **no `opportunity` table** at all, Sales isn't installed here, so `cr9cd_ticketrequest.crm_opportunity` is plain text id/name, not a lookup), `cr9cd_scoringconfig`, `cr9cd_ticketrequest`, `cr9cd_requestcontact` (manual join table — no native N:N via this tool, as anticipated), `cr9cd_assignment`, `cr9cd_waitlistentry`, `cr9cd_attendancerecord`. All wired in via `add-data-source`.

**Concurrency spike result (confirmed, not just theorized)**: checked `src/generated/services/Cr9cd_seatsService.ts` — `update(id, changedFields)` has no conditional/If-Match option, so no compare-and-swap is available from the generated client. Implemented the plan's documented fallback: write-then-detect-then-reconcile (`src/services/assignmentsService.ts` `assignSeat()` — write seat+assignment, re-query active assignments for that seat ordered by `createdon`, keep earliest, auto-decline+waitlist any later duplicate). **Validated live** via direct MCP calls: created a test game/seat/2 requests, created 2 assignment records against the same seat back-to-back — Dataverse allowed both (confirms no platform guard exists, so the client-side fallback is required, not optional) — then confirmed the reconciliation query (filter by seat + active statuses, order by `createdon asc`) correctly identifies the earlier one as winner. Test fixtures deleted after.

**Code added**:
- `src/dataverse/bind.ts` — `bindRef(entitySet, id)` builds `"@odata.bind"` lookup values.
- `src/dataverse/choiceMaps.ts` — two-way maps between domain string enums (`src/domain/enums.ts`) and each table's numeric Dataverse choice codes (recorded here since they're not derivable from the generated model files' `as const` maps, which use display-label keys, not our domain snake_case values).
- `src/services/scoringService.ts` — `getActiveScoringConfig`, `activateScoringConfig` (sequential deactivate-then-activate, no atomicity needed per plan), `createScoringConfig`, `scoreGame` (Dataverse-query rewrite of the old `scoring-service.ts`, calls the verbatim-ported pure engine).
- `src/services/assignmentsService.ts` — `assignSeat` (the guard above), `approveAssignment`, `declineAssignment` (frees seat + promotes waitlist), `reassignAssignment`, `recommendForGame` (score + greedy allocate), `reconcileOrphanSeats` (self-heal orphaned "assigned" seats with no live assignment).
- `src/services/waitlistService.ts` — `addToWaitlist` (idempotent), `promoteWaitlist`.
- `src/services/attendanceService.ts` — `recordAttendance` (upsert), `recomputeContactRollups` (awardedCount = total attendance records, NOT a running counter — rebuilt from history each time, matches the scoring factors' math).

**UI (functional but minimal, not yet the polished/full multi-page design from the old client)**: `src/App.tsx` (HashRouter — chosen over BrowserRouter since Code App hosting may not rewrite arbitrary paths to index.html), `src/pages/TeamsPage.tsx` (teams → seasons → games, inline create forms, auto-creates GA seats on game create), `src/pages/GameDetailPage.tsx` (seats, requests, new-request form, score & rank, recommend, per-request seat assignment, approve/decline, attendance reconciliation). Old Tailwind design system (Badge status-tone map, Card/Field/Modal components, sidebar layout) is explicitly **deferred to Phase 6** — current UI is plain CSS (`src/index.css`), dark-only, no responsive/mobile treatment yet.

Deployed and building clean (`npm run build && npx power-apps push` succeeded). **Not yet click-tested in a real browser** — I have no browser-automation tool available, so the live app (URL below) hasn't been manually driven end-to-end. Recommend the user do a quick click-through of the golden path when they get a chance.

## Phase 3 — DONE (CRM, transfer, integration log; Directory deferred)
- **CRM**: `src/services/crmService.ts` — native Dataverse queries against standard `account`/`contact` tables (added as data sources: `AccountsService`, `ContactsService`). `searchAccounts` (contains-filter on name), `listContactsForAccount` (filter by `_parentcustomerid_value`), `upsertBeneficiaryFromCrmContact` (find-or-create a `cr9cd_contact_beneficiary` row linked via `crm_contact`/`crm_account` lookups). Verified real sample data exists in this env (Contoso, Microsoft accounts). Wired into `GameDetailPage`'s new-request form via `src/components/CrmPicker.tsx` ("Link CRM contact…" — search account, pick contact, auto-fills requester name + links beneficiary).
- **Directory (Office 365 Users) — NOT done, deliberately deferred**: no `office365users` connection exists in this environment (`npx power-apps list-connections` confirmed) and creating one requires interactive OAuth consent in a browser, which can't be done from here. Employee-type requests still work today via manual name entry in the request form — just no live employee-search autocomplete. **To finish this**: go to `https://make.powerapps.com/environments/946e5045-3200-eb26-9160-3fe967a6b21c/connections` → + New connection → "Office 365 Users" → sign in, then re-run `npx power-apps add-data-source -a office365users -c <new-connection-id>` and build a DirectoryPicker component analogous to CrmPicker.
- **Transfer**: `src/services/transferService.ts` — ported the mock/always-succeeds transfer logic as client-side TS (`transferGame(gameId, platform, assignmentIds?)`), wired to a "Transfer approved" button on `GameDetailPage`. No real ticketing-platform integration exists in the original app either, so nothing new needed here.
- **Integration log**: `cr9cd_integrationlog` table created + wired; `src/services/integrationLogService.ts` (`logIntegration`, best-effort/never-throws) called from `transferService`. Not yet called from every other adapter site (CRM/directory) the way the original app's audit trail did — only transfer writes today. No viewer page built (query via Dataverse MCP `read_query` if needed) — lower priority than the CRUD pages.

## Phase 4 — DONE (Excel export)
`src/services/exportService.ts` — `exportSeasonTracker(seasonId)` ports `buildWorkbook()` from the old `adapters/export/xlsx.ts` near-verbatim: one row per request/assignment/attendance combination, red fill for transferred rows, using `exceljs`'s in-memory `writeBuffer()` → `Blob` → synthetic `<a download>` (confirmed this bundles fine for the sandboxed runtime, per the plan's "verify early" note — no SheetJS fallback needed). Wired to an "Export .xlsx" button per season on `TeamsPage`.

## Phase 6 — DONE, with deliberate scope cuts
Built: `DashboardPage` (team/season/game/seat-utilization/request-status KPIs, no charting library — plain stat cards), `ScoringConfigPage` (weight sliders + param fields + version history + activate), `ContactsPage` (list/filter/create + per-contact request+attendance history), `WaitlistPage` (per-game queue viewer). `TeamsPage` already covers team/season/game creation (folds in what would have been separate SeasonDetailPage/GamesPage/EventsPage — "Custom Events" synthetic team already seeded in Phase 1, so custom events are just games created under that team, no separate page needed).

**Deliberately NOT built** (redundant with the above, not requested explicitly, low value relative to effort): a flat all-games list page, a separate Events page, an Integration Logs viewer page, and the old Tailwind design system (Badge status-tone map, Card/Field/Modal/sidebar-layout components) — current UI is plain CSS (`src/index.css`), dark-only, functional but not visually polished. CSV schedule import (the old hand-rolled CSV parser) was also not ported — games/seats are created via the simple inline forms on `TeamsPage`/`GameDetailPage` instead.

## Explicitly deferred to a future dedicated pass (per user's 2026-07-17 request, after talking to their boss)
**All AI Builder / GPT-4.1 Mini work** — narrative generation for the ranking panel, and AI-assisted schedule-paste parsing. Nothing built for this yet; do not start it without the user explicitly asking to pick it up. When that pass happens, see the plan doc's "AI narrative + schedule-parsing → GPT-4.1 Mini" section for the intended approach (Power Automate flow wrapping "AI Builder – Run a prompt", exposed via a connector) and its one open tooling question (whether `/add-connector` can wrap a Power-Automate-triggered flow).

## Real 2026 season data seeded (2026-07-17)
User provided a real spreadsheet (`AVIATOR & LIGHTS TIX.xlsx`) — the actual current-season schedule for the Aviators and Lights, including tickets already claimed by name. Imported into Dataverse directly via the `dataverse-org5c824bd0` MCP server's `create_record` (batch mode, up to 25/call — much faster than one-at-a-time):
- 2 seasons ("Aviators 2026 Season", "Lights 2026 Season"), both linked to their real team records.
- 92 games (75 Aviators + 17 Lights, matching each team's real home-game count) — date/time/opponent/promotions parsed from the sheet; games before 2026-07-17 marked `completed`, later ones `scheduled`.
- 13 contacts, deduped from ~16 raw claimant name variants in the sheet by first-name grouping (e.g. "Tara" and "Tara Taylor" with the same phone merged into one) — all created as customer/gold per the user's choice. Flagged to the user: **SALES, RESERVED, and CRSV** look like internal ticket-bucket labels rather than real people and may want reclassifying.
- 229 seats across all 92 games (2 default per team, or more if a claim needed more — max seen was 4).
- 58 ticket requests for the games that already had a named claimant in the sheet, status `fulfilled` if the game already happened or `approved` if still upcoming.
- 127 seat assignments (one per claimed seat across those 58 requests), status `transferred` for past games or `approved` for upcoming ones.
All counts verified against the DB after creation (229/58/127, zero failed inserts). This means the app now opens showing the real current state of the season instead of an empty shell.

## Edit/delete UI (added 2026-07-17)
User flagged that the app had no way to fix data-entry mistakes — every page was create-only, no edit or delete anywhere, right after a real-data import made that risk concrete. Added:
- `src/services/assignmentsService.ts` — `deleteAssignment()`: hard-delete (vs. the existing soft `declineAssignment`), still frees the seat and promotes the waitlist.
- `src/services/requestsService.ts` (new) — `deleteRequest()`: cascades through active assignments first (freeing their seats) before deleting the request.
- `src/services/gamesService.ts` (new) — `deleteGame()` + `countGameDependents()`: cascades attendance records → waitlist entries → assignments → requests → seats → the game itself, deepest-first so referential integrity never blocks it. Delete-game UI shows the dependent counts in the confirm dialog.
- `ContactsPage.tsx` — inline `EditContactForm` (name/type/tier/company/email/phone) + Delete button per row.
- `GameDetailPage.tsx` — inline `EditGameForm` + Delete game (with dependent-count confirm); a seats table was added (previously only a count was shown) with per-seat Delete (only when `available`, to avoid orphaning a live assignment); requests get inline `EditRequestForm` (name/qty/type/status/notes) + Delete (cascades assignments); each assignment badge gets a hard Delete button alongside the existing Approve/Decline.
Deliberately NOT done: edit/delete for Teams, Seasons, and Scoring Configs — rarely wrong (few rows, versioned, or admin-only) and lower priority than the operationally busy tables above. Revisit if it comes up.
Verified: `npm run build` and `npm test` both clean after the change.

Deployed 2026-07-17 (`npm run build && npx power-apps push`) — live app includes the edit/delete UI and the seeded 2026 season data together.

## Tailwind design system restored (2026-07-17)
User asked for the old client's visual design back, now that Dataverse/CRUD is working (it had been deliberately cut in Phase 6 to focus on function first). Recovered the whole design system from git history (commit `57615aa`, `packages/client/src/components/*` + `tailwind.config.js` + `postcss.config.js` + `index.css`) and ported it in place:
- Installed `tailwindcss@^3`, `postcss`, `autoprefixer`, `clsx`. Added `tailwind.config.js` (AIS brand-red palette, Inter font, `shadow-card`) and `postcss.config.js` at repo root. Rewrote `src/index.css` with `@tailwind` directives + `.card`/`.input`/`.label` component classes.
- Recovered the AIS logo binary asset (`git show 57615aa:packages/client/src/assets/ais-logo.webp > src/assets/ais-logo.webp`) — came through intact (verified via `file`).
- Ported `src/lib/format.ts` (date/currency/score formatters, `titleCase`) — simplified date handling to string-only since Dataverse only ever returns ISO strings (old version also handled unix-ms ints for the old SQLite server).
- Ported design-system components as-is where possible: `Badge` (status→tone map extended with `customer`/`employee`), `Button`, `Spinner`, `EmptyState`, `Field`/`TextInput`/`TextArea`/`Select`/`EnumOptions`, `PageHeader`, `StatCard`, `DataTable`, `ScoreBreakdown` (types already matched `domain/scoring-types.ts` exactly), `Modal` (kept; dropped `Drawer` and `QueryState` from the old set — unused/react-query-specific, nothing in this app needs them).
- `Layout.tsx` — ported the sidebar shell, dropped the old auth/role gating (auth is deferred) and the Games/Events/Admin nav sections (those pages don't exist in this app — Teams/Contacts/Waitlist/Scoring cover it). `App.tsx` now nests all page routes under `<Layout>` via `<Outlet>` instead of rendering a flat topbar nav on every page.
- Restyled all 6 pages (Dashboard, Teams, GameDetail, Contacts, Waitlist, ScoringConfig) and `CrmPicker` to use the new components instead of the plain-CSS classes. All the edit/delete functionality added earlier today was preserved during the restyle.
Verified: `npm run build` (Tailwind CSS compiled to ~24kB, logo asset bundled) and `npm test` both clean.
**Not verified visually** — no browser-automation tool available this session, so I haven't actually seen it rendered. Recommend a real click-through before/after deploying.

Deployed 2026-07-17 (`npm run build && npx power-apps push`) — live app now has the Tailwind design system.

## Admin/manager detection (added 2026-07-17)
User asked to restore "admin/manager detection" from the original GitHub version. **Investigated first and found there was nothing real to restore**: `packages/server/src/middleware/{auth,requireRole}.ts` and `packages/client/src/auth/AuthContext.tsx` in commit `57615aa` all have comments stating auth was "intentionally removed" — `requireAuth` injected a hardcoded admin user, `requireRole` was a pass-through, `AuthContext`'s `hasRole` always returned `true`. Told the user this plainly rather than silently building something and calling it a restoration.

Asked the user how to determine admin status now (no real prior logic existed) — they chose **Dataverse security roles** (checking the signed-in user's actual role, e.g. System Administrator/System Customizer) over a custom admin-list table.

Implementation:
- Added 3 standard (non-custom) Dataverse tables as data sources: `systemuser`, `systemuserroles`, `role` (via `add-dataverse` skill, same `npx power-apps add-data-source -a dataverse -t <table> -u <org-url>` pattern as everything else). Generated services: `SystemusersService`, `SystemuserrolescollectionService`, `RolesService`.
- `src/services/authService.ts` — `getCurrentUser()` (memoized for the session): calls `getContext()` from `@microsoft/power-apps/app` to get the real signed-in Entra identity (`fullName`, `userPrincipalName`, `objectId`), matches `objectId` to `systemuser.azureactivedirectoryobjectid`, looks up their roles via the `systemuserroles` intersect table, and checks for `System Administrator`/`System Customizer` in `role.name`. Validated the underlying query chain live via MCP against the real environment before writing app code (confirmed "Ethan Mikes" has System Administrator).
- `src/auth/AuthContext.tsx` — `AuthProvider` + `useAuth()` (mirrors the old app's file location/API shape, but for real this time).
- `src/components/RequireAdmin.tsx` — gates a route, shows an `EmptyState` for non-admins.
- Wired into `App.tsx` (wraps the router in `AuthProvider`, wraps the `/scoring` route in `RequireAdmin`) and `Layout.tsx` (hides the "Scoring Engine" nav item for non-admins, shows the signed-in user's name + an Admin/Member badge in the header).
**Important caveat told to the user**: this is a UI-level gate only. It hides the page and nav link from non-admins, but does not restrict the underlying Dataverse tables themselves — anyone with access to the environment can still query/write `cr9cd_scoringconfig` directly. A real data-layer lockdown would need actual Dataverse table/column security roles on the custom tables, which is a separate, bigger effort the old app never had either.
Verified: `npm run build` and `npm test` clean. Not yet deployed as of this entry.

**Follow-up (same day)**: checked Rosa Solares (`rsolares@ais-now.com`) against the real role check — she currently has **zero** Dataverse security roles, so she'd fail it. Also confirmed `emikes@ais-now.com` ("Ethan Mikes") genuinely DOES hold System Administrator in Dataverse (verified directly via MCP `read_query`), so if the live app isn't showing them as admin, the real role check itself isn't the problem — something about how the live signed-in identity resolves is. User asked for a debug override for both anyway (not a real Dataverse role grant for Rosa — asked and they chose the low-blast-radius option). Added `DEBUG_ADMIN_EMAILS = ['emikes@ais-now.com', 'rsolares@ais-now.com']` to `src/services/authService.ts`, OR'd into `isAdmin` alongside the real role check, with a new `isDebugAdmin` flag surfaced in the header badge ("Admin (debug)") so it's visibly flagged rather than silently indistinguishable from a real admin. **This is explicitly temporary — remove `DEBUG_ADMIN_EMAILS` once real role-based detection is confirmed working for the actual signed-in users.**

Deployed 2026-07-17 (`npm run build && npx power-apps push`) — live app now includes admin/manager detection + the debug overrides above.

## Contacts page: History/Edit converted from inline popup to Modal (2026-07-17)
User flagged the Contacts page as "horrible" — specifically that the History and Edit buttons opened inline popups/row-swaps that couldn't be closed (History was an absolutely-positioned `<div>` with no outside-click or ESC handling, only re-clicking the same toggle closed it, and it could get clipped by the table's `overflow-x-auto` wrapper; Edit swapped the whole row into an inline form with only Save/Cancel, no way to back out via ESC or clicking away). Converted both to use the existing `Modal` component (`src/components/Modal.tsx`, already supports ESC + backdrop-click + close button) instead of building a new popover pattern. `EditContactForm` split into `EditContactModal` (wraps `Modal`, open state driven by `editingId`) + `EditContactFields` (the actual form, mounted fresh each time the modal opens so field state initializes straight from props with no `useEffect` needed — avoided a `react-hooks/set-state-in-effect` lint error this way). `ContactHistory`'s popover became a `Modal` too, fetching history data on open instead of via toggle.
Verified: `tsc --noEmit` clean, `eslint` clean (only a pre-existing `load()`-in-`useEffect` pattern remains, identical to ones already in `TeamsPage.tsx`/`WaitlistPage.tsx` — not introduced by this change). **Not visually browser-tested** — this is a real Power Apps Code App requiring live Dataverse auth to populate the contacts table, no local mock/browser-automation tool was available this session.

Deployed 2026-07-17 (`npm run build && npx power-apps push`) — live app now has the Modal-based History/Edit.

## Current overall status: feature-complete except AI Builder (deferred) and Office365Users directory search (needs a one-time user action)
All phases built, tested (`npm run build` + `npm test` clean), and deployed (`npx power-apps push` succeeded) as of 2026-07-17. Live app: see URL at top of this file.

**Not yet done by me**: a real browser click-through of the deployed app (no browser-automation tool available this session) — logic was validated via direct Dataverse MCP calls (see Phase 2 concurrency test) and `tsc`/`vitest`, not by driving the actual UI. Recommend the user do a manual pass over the golden path (create team → season → game → seats → request → CRM-link a request → score & rank → assign → approve → transfer → attendance → export .xlsx) when convenient.

## Admin/manager detection simplified to "everyone is admin" (2026-07-17)
User asked to (for now) make everyone using the app admin, and remove the Admin/Member badge from the header. Simplified `src/services/authService.ts`: dropped the Dataverse role-lookup chain (`SystemusersService`/`SystemuserrolescollectionService`/`RolesService` queries, `ADMIN_ROLE_NAMES`, `DEBUG_ADMIN_EMAILS`, `isDebugAdmin`) — `getCurrentUser()` now just resolves the signed-in identity via `getContext()` and always returns `isAdmin: true`. Removed the header `Badge` (Admin/Member/"Admin (debug)") from `Layout.tsx`. `RequireAdmin.tsx` and the `adminOnly` nav gating were left in place (harmless no-ops now that `isAdmin` is always true) rather than ripped out, since this is explicitly temporary. Real role-based detection can be restored later by reverting this change (git history has the prior version) if/when the org wants tiered access again.
Verified: `tsc --noEmit` clean.

Deployed 2026-07-17 (`npm run build && npx power-apps push`) — live app now treats every signed-in user as admin, no badge shown.

## Kind-aware games/events + schedule template import/export (2026-07-17)
User wanted events (kind=Event, "like a movie", no opponent) properly distinguished from games (kind=Game, has an opponent) in the UI, plus season-scoped bulk schedule import/export. Investigated first: `cr9cd_game` already had a `cr9cd_kind` choice column (Game/Event) and a `cr9cd_title` field from Phase 1 — nothing was actually wired to use them (every form only ever wrote `cr9cd_opponent`/kind='game'). Decided against splitting into two Dataverse tables (asked the user; chose to keep one table, enforce via UI) since the season lookup already covers both kinds through the shared table.
- `NewGameForm`/`EditGameForm` (`TeamsPage.tsx`/`GameDetailPage.tsx`) now have a Kind select; the single name field is labeled "Opponent" or "Event title" depending on kind and writes to `cr9cd_opponent`/`cr9cd_title` respectively (clearing the other).
- Every place that displayed `vs {opponent}` (TeamsPage game list, GameDetailPage header, WaitlistPage game picker, exportService's Season Tracker rows) now branches on kind and shows the event title instead when applicable.
- New `src/services/scheduleTemplateService.ts`: `exportScheduleTemplate(seasonId)` downloads an .xlsx (Date/Kind/Opponent-Title/Promotions/Notes/Total Seats/Status, one row per existing game, hidden ID column for round-tripping) — a lighter-weight schedule-only template, deliberately separate from the existing `exportSeasonTracker` (which reports on requests/assignments/attendance, not raw schedule). `importScheduleTemplate(seasonId, file)` reads a re-uploaded copy of that template via `exceljs`'s `workbook.xlsx.load(await file.arrayBuffer())`: rows with a blank ID create a new game/event (auto-creating GA seats from the Total Seats column, same convention as the inline form); rows with an ID matching an existing game in that season update it; rows with an ID that doesn't belong to the season are rejected as errors rather than silently applied. Never deletes — rows removed from the sheet are just left alone. Returns `{created, updated, errors}` surfaced inline in the UI.
- Wired into `TeamsPage.tsx` as "Download template" / "Import schedule" buttons per season, next to the existing "Export .xlsx" (Season Tracker) button.
Verified: `tsc -b`, `npm run build`, and `npm test` all clean. **Not yet browser-tested** (no browser-automation tool available this session, same limitation as prior UI work) — recommend the user try downloading a template for a real season, editing a row, and re-importing it before relying on this for real schedule changes. Not yet deployed as of this entry — run `npm run build && npx power-apps push` to ship it.

## Contacts as required requestors + one-click ticket assignment (2026-07-17)
User wanted the Contacts table (`cr9cd_contact_beneficiary`) to actually be the requestor on every ticket request (previously `cr9cd_requester_name` was free text with only an *optional* CRM-linked contact), and wanted to "assign tickets" without picking an individual seat. Asked first: confirmed (1) require a Contact for every request rather than keeping free-text as a fallback, and (2) keep the seat table as-is (still backs capacity/ADA/attendance tracking, and is GA/general-admission inventory, not literal reserved seats) but make assignment a single action for the full outstanding quantity instead of one click per seat.
- New `src/components/ContactPicker.tsx`: type-ahead search over `cr9cd_contact_beneficiary` (contains-filter on name, same debounce pattern as the existing `CrmPicker`), with an inline "New contact" quick-create fallback for someone not in the system yet. Returns `{id, name, type}`.
- `src/dataverse/bind.ts` gained `escapeODataString` (was a private duplicate inside `crmService.ts` — hoisted so `ContactPicker` could reuse it; `crmService.ts` now imports it instead of redefining it).
- `GameDetailPage.tsx`'s `NewRequestForm` now requires picking a contact via `ContactPicker` before the rest of the form (quantity/type/sales-opp) appears; `'cr9cd_Beneficiary_Contact@odata.bind'` is always set. The old "Link CRM contact…" flow is preserved as an alternate "Import from CRM instead…" path — it still calls `upsertBeneficiaryFromCrmContact` to find-or-create the beneficiary row, then feeds that back into the same required-contact slot (so the CRM path also satisfies the requirement rather than being a separate optional add-on).
- `EditRequestForm`'s requester-name field is now read-only display text (was a free-text input) — since the name is sourced from the linked Contact, editing it in place would drift the two out of sync; to fix a wrong requestor, delete and recreate the request.
- New `assignOutstandingTickets()` in `src/services/assignmentsService.ts` — walks the available-seat list and calls the existing `assignSeat()` (unchanged, still does the write-then-detect-then-reconcile double-booking guard per-seat) until the request's full outstanding quantity is covered or inventory runs out. `GameDetailPage`'s per-request "Assign seat" button became "Assign N ticket(s)" — one click assigns everything outstanding for that request; if seats run out partway, a message reports how many were actually assigned instead of throwing.
Considered and rejected (per the user's answer): dropping seat-level records entirely in favor of a plain ticket-count field — would have required reworking the waitlist double-booking guard and attendance reconciliation, which key off individual seat/assignment rows; the one-click UI change achieves the stated goal ("don't make me pick a seat") without that blast radius.
Verified: `tsc -b`, `npm run build`, and `npm test` all clean. `ContactPicker`'s debounced-search `useEffect` has the same pre-existing `react-hooks/set-state-in-effect` lint pattern already present in `CrmPicker.tsx`/`TeamsPage.tsx`/`WaitlistPage.tsx`/etc. (10 total instances of this same accepted pattern now, was 9) — consistent with existing convention, not a new category of issue. **Not yet browser-tested.** Not yet deployed as of this entry.

## Real CRM (accounts/contacts/opportunities) via cross-org Dataverse connector (2026-07-17)
User's feedback: needed to search real CRM accounts and assign contacts *and opportunities* (with the opportunity's "Manual Rep Credit") from that account. Turned out the real CRM data (accounts like "FAST TOWING SAN DIEGO", "NeoVolta", real opportunities) lives in a **separate Dataverse environment** — `DynamicsCRM` (Production, `https://org83e945f6.crm.dynamics.com/`) — not `AIS Development` (`https://org5c824bd0.crm.dynamics.com/`, this app's published environment). Native `-a dataverse` table binding only reaches the published environment, and moving the whole Code App to publish in DynamicsCRM was ruled out as too disruptive.
**Workaround (user-suggested, verified live before building anything on it, per their explicit ask to spike first)**: skip native table binding; add the **Microsoft Dataverse connector** (`shared_commondataserviceforapps`, apiId `commondataserviceforapps`) as a connector-based data source instead of a native one — `npx power-apps add-data-source -a commondataserviceforapps -c 6180b51c3b584863a0b902452189152d` (reused the existing "emikes@ais-now.com" connection, found via `npx power-apps list-connections` — no new interactive OAuth consent needed, unlike the still-deferred Office365Users case). This generates `src/generated/services/MicrosoftDataverseService.ts` with actions like `ListRecordsWithOrganization(organization, entityName, ..., $select, $filter, ..., $top)` that take an explicit target org URL per call — confirmed this exact method exists and works by calling it live (read-only) against DynamicsCRM's `accounts` table from a local dev session and getting back real rows.
- **Local dev workflow correction**: `npx power-apps run` is NOT the right way to test locally — it collides with other apps' ports and isn't the documented flow. The correct duo is `npm run dev` (Vite, prints its own "Local Play" URL via the Power Apps Vite plugin) + `pac code run` (sets up the local connection proxy, one-shot, "Connected as emikes@ais-now.com"). Use the Vite-plugin-printed Local Play URL, not a hand-built one.
- Discovered the target org URL via `pac org list`/`pac admin list` (no need to ask the user) — this tenant has 6 Dataverse orgs total (AIS, AIS (default), AIS Development, Dynamics Test, **DynamicsCRM**, DynamicsSandbox).
- Discovered the exact "Manual Rep Credit" field name (`ais_manualrepcredit`) by querying one real opportunity record with no `$select` restriction and reading the full field list back — confirmed it matches the old app's code comment (`ais_manualrepcredit`) rather than guessing.
- `src/services/crmService.ts` rewritten: `searchAccounts`/`listContactsForAccount` now call `MicrosoftDataverseService.ListRecordsWithOrganization` against DynamicsCRM instead of the local native `AccountsService`/`ContactsService` (per the user's choice to replace local search entirely, not keep it as a fallback — those local AISDEV account/contact tables were only ever a connectivity probe with sample-ish data anyway). Added `listOpportunitiesForAccount(accountId)` (filters `_customerid_value eq {accountId} and statecode eq 0`, the standard polymorphic "Potential Customer" lookup). The native `AccountsService`/`ContactsService` generated files and their `power.config.json` data source entries were left in place (unused, harmless) rather than removed via `delete-data-source` — no functional reason to touch them.
- `upsertBeneficiaryFromCrmContact` can no longer bind the beneficiary's `Crm_Contact`/`Crm_Account` lookups to the real CRM records (those lookups only accept records from the *local* Dataverse org, and the real contact/account now live in a different org entirely) — repurposed the existing-but-unused `cr9cd_directory_user_id` text field to stash the cross-org CRM contact id for find-or-create dedup instead. That field was originally reserved for a future Office365Users/Entra employee-directory integration (still deferred, never built) — flagging in case that work picks up later and needs a distinct field instead of colliding conventions.
- `src/components/CrmPicker.tsx` rewritten: after picking an account, shows contacts and opportunities side by side (fetched in parallel) instead of just contacts. Contact selection is required (feeds the `NewRequestForm`'s required-contact slot from the earlier requestor change); opportunity selection is optional and shows Manual Rep Credit or estimated value inline. A "Confirm selection" button finalizes both at once. `CrmSelection` now carries `{account, contact, opportunity}`.
- `GameDetailPage.tsx`'s `NewRequestForm`: picking an opportunity via the CRM import path now sets `cr9cd_crm_opportunity_id`/`cr9cd_crm_opportunity_name` (previously-unused fields already on the schema from Phase 2) and prefills the "Sales opp $" field from the opportunity's Manual Rep Credit (falling back to estimated value) — still editable before submitting.
- Validated with a temporary, clearly-marked spike page (`CrmSpikePage.tsx` + a temporary sidebar "Debug" nav link) that made two read-only calls against DynamicsCRM, confirmed real data came back, then was deleted along with its route/nav entry once done — nothing temporary left in the app.
Verified: `tsc -b`, `npm run build`, `npm test` all clean. Lint: 2 new findings, both in the auto-generated `src/generated/models/MicrosoftDataverseModel.ts` (Microsoft's own codegen output for the connector, marked "do not edit directly") — not in any hand-written code, so left as-is like the other pre-existing generated/legacy lint noise already in this repo. Not yet tested for the full end-to-end request-creation flow with a real opportunity selected — only the underlying connector reads were validated live.

Deployed 2026-07-17 (`npm run build && npx power-apps push`) — live app now includes kind-aware games/events, the schedule template import/export, required-contact requestors + one-click ticket assignment, and the cross-org CRM (accounts/contacts/opportunities) search, all together.

## Season edit/delete + move game/event between seasons (2026-07-17)
User clarified the season model in their own words: seasons are containers that hold both a team's game schedule and standalone custom events, and asked to be able to edit/delete seasons and move a game or event into a different season. This was the "deliberately NOT done" gap flagged in the earlier Edit/delete UI entry above (seasons were skipped as "rarely wrong, admin-only, lower priority").
- New `src/services/seasonsService.ts` — `countSeasonDependents(seasonId)` + `deleteSeason(seasonId)`: cascades by calling the existing `deleteGame()` (from `gamesService.ts`) on every game/event in the season first — deepest-first, same convention as `deleteGame` itself — then deletes the season row. Delete-season UI shows the game/event count in the confirm dialog, same pattern as delete-game.
- `TeamsPage.tsx` — `TeamSeasons` gained an inline `EditSeasonForm` (name/start date/end date/status) toggled by an "Edit" button per season card, and a "Delete" button next to it. Lifted season loading up to the top-level `TeamsPage` component (`allSeasons`, unfiltered by team) so a season move that crosses teams refreshes correctly on both the source and destination team's card, instead of each `TeamSeasons` owning an independent per-team season query as before.
- New `MoveGameSelect` component on `TeamsPage.tsx` — a compact per-row `<select>` next to each game/event in a season's list, populated with every season across every team (`"{team} — {season}"`), that reassigns `cr9cd_Season@odata.bind` on change. Lets you move a game or event into a different season directly from the schedule list, without opening the game detail page.
- `GameDetailPage.tsx`'s `EditGameForm` also gained a "Season" field (same full cross-team season list) for reassigning from the detail page, plus the game header's subtitle now shows the current season name (`cr9cd_seasonname`) alongside promotions, since the season is no longer a fixed, invisible attribute now that it's editable.
Verified: `tsc --noEmit`, `npm run build`, and `npm test` all clean. Lint: 1 new `react-hooks/set-state-in-effect` finding in `TeamsPage.tsx`'s new top-level teams+seasons load effect — same pre-existing accepted pattern already flagged elsewhere in this file (now common across `TeamsPage.tsx`/`GameDetailPage.tsx`/`WaitlistPage.tsx`/`CrmPicker.tsx`), not a new category of issue. **Not yet browser-tested** (no browser-automation tool available this session) — recommend a manual click-through of edit/delete season and the move-game dropdown before relying on it.

Deployed 2026-07-17 (`npm run build && npx power-apps push`) — live app now includes season edit/delete and the move-game/event-to-season UI.

## Two bugs fixed in the season edit/delete + move UI (2026-07-17)
User tested the above live and reported: (1) the move-to-season dropdown was unclickable — the click "fell through" to whatever was underneath and navigated away; (2) picked the 18th as a season start date but it displayed as the 17th.
- **Unclickable dropdown**: root cause was nesting a `<select>` (`MoveGameSelect`) inside the row's `<Link>` in `TeamsPage.tsx` — interactive content nested inside an anchor is invalid HTML and browsers handle clicks on it inconsistently (the `onClick`/`onChange` `stopPropagation()` calls I'd added didn't reliably stop the anchor's navigation). Fixed by restructuring the row: the `<Link>` now wraps only the date/name text, and the badge + `MoveGameSelect` sit as siblings in a plain `<div>` row instead of inside the anchor. Removed the now-unnecessary `stopPropagation()` calls from `MoveGameSelect`.
- **Timezone off-by-one**: `EditSeasonForm`'s start/end date fields used `<input type="date">` (a plain "YYYY-MM-DD" value), and `new Date("2026-07-18").toISOString()` parses date-only strings as **UTC midnight** — in any timezone behind UTC, that instant displayed back via `.toLocaleDateString()` falls on the 17th locally. Added a symmetric pair of date-only helpers to `src/lib/format.ts`: `dateOnlyToIso()` (parses the input as **local** midnight via an explicit `T00:00:00` suffix, avoiding the UTC branch) and `isoToDateOnlyInput()` / `formatDateOnly()` (read back via local `getFullYear/getMonth/getDate`, not UTC getters, so the round-trip is symmetric regardless of whether the timezone offset is ahead of or behind UTC). `EditSeasonForm`'s initial values and save() now use these instead of raw `.slice(0,10)`/`new Date(...).toISOString()`; the season card's date range display uses `formatDateOnly()` instead of `new Date(...).toLocaleDateString()`. Note: this only fixes dates saved going forward — the 2 real seasons seeded earlier this session may still display off-by-one until their start/end dates are re-saved through the edit form.
Verified: `tsc --noEmit`, `npm run build`, `npm test` all clean.

Deployed 2026-07-17 (`npm run build && npx power-apps push`) — live app now has both fixes.

## Same timezone bug also in the "Add game" form (2026-07-17)
User tested again: created a game with the date set to the 18th, still showed the 17th. The season-date fix above didn't cover this — `NewGameForm` (the "Add game" quick-create on `TeamsPage.tsx`, used for both games and events) has its own `<input type="date">` + `new Date(date).toISOString()` conversion, the identical UTC-midnight bug, just on `cr9cd_game_date` instead of the season's start/end date. (`EditGameForm` on `GameDetailPage.tsx` was already fine — it uses `datetime-local`, which parses as local time by spec, no bug there.) Fixed by swapping in the same `dateOnlyToIso()` helper from the season fix. Grepped the whole `src/` tree for other `type="date"` inputs first — confirmed these three (both season fields, already fixed, plus this one) were the only ones.
Verified: `tsc --noEmit`, `npm run build`, `npm test` all clean.

Deployed 2026-07-17 (`npm run build && npx power-apps push`) — live app now has all three date fields fixed.

## Standalone Games/Events tabs, team CRUD, and modal pattern restored for parity with the first commit (2026-07-17)
Traced a run of contradictory stakeholder feedback ("seasons contain events which are custom events or games, move games/events between seasons" vs. "make seasons a separate table related to either games or events") back to the **first commit**'s nav, which had `TeamsPage` (drill-down), a standalone flat `GamesPage`, and a standalone flat `EventsPage` as three separate tabs — later feedback rounds were each describing a fragment of that original three-tab structure, not a real schema disagreement. Closed the remaining gaps against the first commit:
- **Standalone Games/Events tabs**: new `src/pages/GamesPage.tsx`/`EventsPage.tsx` — flat, cross-team `DataTable` of `cr9cd_games` filtered by `cr9cd_kind` (game vs. event), status filter, row click → existing `/games/:gameId`. Team name resolved client-side via a `seasonId → cr9cd_teamname` map (the games table only denormalizes season name, not team name). Wired into `App.tsx` (`/games`, `/events`) and `Layout.tsx` nav (Inventory section, original icons/labels).
- **Team CRUD**: new `src/services/teamsService.ts` (`countTeamDependents`/`deleteTeam`, cascades through `deleteSeason` per season same as `deleteSeason` cascades through `deleteGame`) — this was flagged as "deliberately not done" in the earlier Edit/delete UI pass and never revisited. `TeamsPage.tsx` gained a "New team" button (header) and a per-team "Settings" button opening a modal with the full field set (name/abbreviation/sport/venue/official URL/default tickets) + delete-with-dependent-count-confirm, mirroring the original `NewTeamModal`/`TeamSettings`.
- **Modal interaction pattern**: `TeamsPage.tsx`'s season create/edit and add-game flows were inline-expand-in-place; converted to `Modal`-based dialogs (`CreateSeasonModal`, `EditSeasonModal`, `AddGameModal`) using the existing `Modal` component (already used elsewhere — Contacts page History/Edit), matching the original's modal-drawer feel instead of edit-in-place. `MoveGameSelect` (a new capability the original never had) stayed as a compact inline `<select>` — no modal precedent to match since it doesn't exist in the first commit.
Deliberately still not restored (separate, lower-priority gaps, discussed with the user): the AI paste-and-parse schedule import (explicitly deferred to the AI Builder pass) and the top-level team-list-as-`DataTable`-with-click-to-open-drawer browsing pattern (current always-expanded team cards were kept as-is — restructuring the whole page layout wasn't part of this pass).
Verified: `tsc -b`, `npm run build`, `npm test` all clean. Lint: one more instance of the pre-existing accepted `react-hooks/set-state-in-effect` pattern in the new `GamesPage.tsx`/`EventsPage.tsx` (now common across ~10+ files), not a new issue category. **Not browser-tested** (no browser-automation tool available this session) — recommend a manual click-through of New team, Settings/delete team, the modal-based season/game creation, and the new Games/Events tabs.

Deployed 2026-07-17 (`npm run build && npx power-apps push` succeeded) — live app now includes standalone Games/Events tabs, team CRUD, and the modal-based season/game creation flow.

## Topbar "Export to Excel" restored (2026-07-17)
User noticed the first commit's topbar had a global "Export to Excel" button (`Layout.tsx` → `ExportButton` with no `seasonId`) that never made it into the port — only the per-season export on `TeamsPage` was built. Confirmed via `git show 57615aa` that the original's `/season.xlsx` server route treated a missing `seasonId` as "no filter," combining every team/season's games/requests/assignments/attendance into one workbook; `ExportButton` was mounted exactly once, in the header, never per-season.
- `src/services/exportService.ts` — `exportSeasonTracker(seasonId?: string)`: `seasonId` is now optional. Given, behaves as before (single season). Omitted: fetches every game across every season/team with no filter, and resolves `team`/`season` labels per row via a `seasonId → {teamname, name}` map (built from one `Cr9cd_seasonsService.getAll()` call) instead of the single season lookup. Added `Team` and `Season` as the first two workbook columns (needed once a single sheet can span multiple teams — the season-scoped export didn't need them since the season was already the file's context). Filename: `season-tracker-${fileLabel}.xlsx` where `fileLabel` is the season name (scoped) or `all-seasons` (global).
- `src/components/Layout.tsx` — new `TopbarExportButton` in the header, next to the user name, calling `exportSeasonTracker()` with no argument. Same icon/label ("Export to Excel") as the original's `ExportButton`.
Verified: `tsc -b`, `npm run build`, `npm test` all clean. **Not browser-tested** (no browser-automation tool available this session) — recommend confirming the topbar button produces a multi-team workbook with correct Team/Season columns before relying on it.

Deployed 2026-07-17 (`npm run build && npx power-apps push` succeeded) — live app now has the global topbar Export to Excel button alongside the existing per-season export.

## Requests tab restored (2026-07-17)
User asked for the first commit's standalone Requests tab back. The original `RequestsPage.tsx` (408 lines) was a flat, cross-game table of every ticket request (filterable by status), plus a "New request" modal (pick team+opponent or event first, then CRM/Directory contact picker, quantity, revenue, notes) and an "Import from email" modal (pasted raw email text parsed server-side by an intake adapter).
- New `src/pages/RequestsPage.tsx` — flat `DataTable` over `cr9cd_ticketrequests` (no game scoping), status filter via `REQUEST_STATUS`, columns (Requester/Company, Game — linked via the denormalized `cr9cd_gamename` to `/games/:gameId`, Type, Qty, Sales opp $, Score, Status). "New request" opens a modal: a Game/Event `<select>` (scheduled games+events across all teams, same team-name-join pattern as `GamesPage`/`EventsPage`), then the identical contact-selection flow already used in `GameDetailPage`'s per-game `NewRequestForm` (`ContactPicker` required-contact search, "Import from CRM instead" via `CrmPicker` + `upsertBeneficiaryFromCrmContact`, quantity/sales-opp/notes) — duplicated rather than extracted into a shared component since the two forms differ in whether the game is fixed or picked, and this was a bounded, one-off addition.
- Wired into `App.tsx` (`/requests`) and `Layout.tsx`'s Distribution nav section, ahead of Contacts (matches original order).
- **Deliberately not restored**: "Import from email" — the original's email-intake adapter did server-side text parsing to extract requests from pasted email text; there's no equivalent in this app and no email/AI integration exists yet. Same category as the already-deferred AI schedule-paste parser — flagging for the same future AI Builder pass rather than building a parallel one-off parser now.
Verified: `tsc -b`, `npm run build`, `npm test` all clean. Lint: 2 more instances of the pre-existing accepted `react-hooks/set-state-in-effect` pattern (now ~12+ across the codebase), not a new issue category. **Not browser-tested** (no browser-automation tool available this session) — recommend a manual pass: filter by status, create a request against a picked game via both the direct-contact and CRM-import paths, and confirm the Game link lands on the right `GameDetailPage`.

Deployed 2026-07-17 (`npm run build && npx power-apps push` succeeded) — live app now has the standalone Requests tab.

## Events page "New event" button restored (2026-07-17)
User caught that the standalone `EventsPage.tsx` built earlier today had no create button — the original's `EventsPage` did (admin-gated, but role gating was a confirmed no-op in the original anyway), opening a `NewEventModal` (title/description/date-time/tickets). Difference from the original worth noting: the original's `Game`/event row could apparently go without a fixed team (an implicit "Custom Events" bucket server-side); this app's `cr9cd_games` requires a non-optional `cr9cd_Season@odata.bind`, so there's no team-less path here — added a required Season picker to the modal instead (same team-name-joined season list used elsewhere).
- `EventsPage.tsx` gained a "New event" button (header) opening `NewEventModal`: Season select, Title, Description (`cr9cd_description`, a field that existed on the schema since Phase 1 but was never surfaced in any form), Date & time (`datetime-local`, same as the already-fixed `EditGameForm` — no timezone bug), Number of tickets (auto-creates GA seats, same convention as `AddGameModal`/`NewGameForm`). Kind is fixed to `event`, no toggle, since this page is events-only.
- Lifted `EventsPage`'s season load into a shared `load()` so the page's own status filter, the new modal's season list, and post-create refresh all stay in sync from one fetch.
Verified: `tsc -b`, `npm run build`, `npm test` all clean. **Not browser-tested** — recommend confirming a created event shows up immediately in the events list and lands correctly on its `GameDetailPage`.

Deployed 2026-07-17 (`npm run build && npx power-apps push` succeeded) — live app now has the New event button on the Events tab.

## AI-assisted schedule-paste import restored, via the environment's AI Prompt cloud flow (2026-07-17)
User asked to restore the original's AI schedule-paste parsing (`ScheduleImportModal`, deferred since the Phase-3 pivot to Dataverse — see the entry above dated earlier today), pointing at an existing cloud flow as the AI provider instead of a direct Anthropic call (this environment has no Anthropic access, matching the original migration plan's decision to use AI Builder instead — `C:\Users\emikes\.claude\plans\async-munching-crab.md`).
- **Found the flow**: queried the Dataverse `workflow` table (`category=5` Modern Flow) for anything prompt-related, found "PowerAppV2 -> Run a prompt,Parse JSON,Respond to a Power App or flow" (Activated). `npx power-apps list-flows` gave the correct Flow ID (`e6f23153-94ba-3883-0a1b-03c3d5e5fcb7`) for CLI purposes — distinct from the Dataverse `workflowid`, don't confuse the two.
- **Wiring spike, matches the plan doc's flagged open question**: first tried `npx power-apps add-data-source -a shared_logicflows -c <connection-id> -t <flow-id>` — this DOES work but only exposes the generic Flow Management API (`WorkflowTriggers_Run(triggerName, api-version)`, no body param, 202-Accepted fire-and-forget, confirmed by reading its swagger at `.power/schemas/logicflows/logicflows.Schema.json` — no `in: body` parameter defined at all). Backed this out (`npx power-apps delete-data-source -a shared_logicflows -n logicflows -f`, deleted the generated files). **The actually-correct mechanism, newer than what the plan doc anticipated**: `npx power-apps list-flows` (lists solution cloud flows invokable from Power Apps) + `npx power-apps add-flow -f <flow-id>` — this generates a flow-specific service (`PowerAppV2__Runaprompt_ParseJSON_RespondtoaPowerApporflowService.Run(input: {text: string})`) with the flow's *real* PowerApps(V2) trigger input schema and its "Respond to a Power App or flow" output schema (`{result?, promptresponse?, predicitonoutput?, json_body?, json_deliverable?}` — several possible field names since the flow's own AI Builder step could populate any of them depending on how it was authored).
- New `src/services/aiScheduleImportService.ts`: `extractScheduleFromText(teamName, rawText)` — builds one combined prompt string (the original's verbatim `PARSE_SYSTEM` rules text + team name + raw pasted schedule, truncated to 60k chars same as before) since the flow's trigger takes a single `text` field, not separate system/user messages like the direct Anthropic call did. Calls `...Service.Run({text: prompt})`, then reads whichever output field is populated (`result ?? promptresponse ?? json_deliverable ?? json_body ?? predicitonoutput`) since which one the flow actually fills isn't yet confirmed by a live call. `parseGames()` (fenced-code-block-stripping, tolerant array extraction, silently drops malformed entries) ported verbatim from `packages/server/src/adapters/schedule/importer.ts`.
- `TeamsPage.tsx` — new `AiScheduleImportModal` (paste text → ticket mode toggle per-game/total-spread-evenly, same UX as the original → preview table flagging already-existing games as duplicates by date+opponent → import creates games + GA seats), wired as an "Import (AI)" button per season card, alongside the existing xlsx-template import and "Add game".
Verified: `tsc -b`, `npm run build`, `npm test` all clean. **The one real unknown**: which output field the flow actually returns text in — untestable without a live call (no browser-automation tool this session), so the fallback chain is a best-effort guess based on the field names the schema exposed, not a confirmed behavior. **First thing to check live**: paste a real schedule into "Import (AI)" on any season and confirm it parses — if it comes back empty/wrong, the fix is almost certainly just reordering or fixing the field read in `extractResponseText()`, not a deeper problem.

Deployed 2026-07-17 (`npm run build && npx power-apps push` succeeded) — live app now has AI-assisted schedule-paste import via the AI Prompt cloud flow.

To redeploy at any point: `npm run build && npx power-apps push` from the project root.

---

Deployed 2026-07-20 (`npm run build && npx power-apps push` succeeded) — live app now includes the 13 features ported from the teammate's `main` branch onto this Code App (`code-app` branch). Summary:
- **Code-only**: waitlist move-to/promote-from + show-all-games; admin delete requests/contacts; dashboard upcoming-games availability card; scoring load-archived-config + delete-non-active; Notification Manager mock page (`/notifications`, "Communications" nav).
- **Schema + code** (6 Dataverse changes applied to `org5c824bd0` via Web API, generated code hand-synced in the per-table style — pac's newer consolidated codegen was incompatible and reverted): new `cr9cd_reservation` table (seat reservations w/ lazy auto-expiry); `cr9cd_ticket_type` on seat; `cr9cd_account_owner` on ticketrequest; season `Completed` status; attendance `Cancelled` outcome; integration adapter `Notification`.
- **Notes**: seat model kept row-per-seat (ticket_type as a seat column, not a count pool); reservation game lookup is parental/cascade, seat lookup referential-restrict (Dataverse single-parent limit); F6 "free-text employee name" was N/A (Code App already uses free contact selection); account-owner surfacing depends on the cross-org connector returning `_ownerid_value` formatted values.
- **Verified**: `tsc -b`, `npm run build`, `npm test` (5/5) all clean. NOT yet browser-click-tested end-to-end at deploy time.
